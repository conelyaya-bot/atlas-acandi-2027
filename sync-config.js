/* URL del servidor de sincronización compartido (celular + web usan la misma).
   Es un túnel HTTPS hacia el servidor Python de sync/. Si cambia la URL del
   túnel (al reiniciar cloudflared), actualiza este archivo en html/ y web/
   y vuelve a publicar. Mientras el túnel esté activo, todos los
   dispositivos comparten los mismos simpatizantes. */
window.SYNC_URL = "https://additional-civilization-dispatched-lover.trycloudflare.com";
